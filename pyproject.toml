[project]
name = "deploy_benchmark"
version = "0.1.0"
description = "LLM 模型部署评测 — 覆盖 vLLM / TensorRT / ONNX Runtime / llama.cpp / OpenVINO"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "likaicheng" },
]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
